# 明新科技大學 校園導覽與線上點餐系統 — 專案設計步驟與開發過程

## 專案設計步驟
研究「3D 虛擬校園導覽與線上點餐系統」的開發方法涉及到許多方面，包括平台設計、全景製作、程式開發、測試、部署以及資料庫管理等。以下是使用 Visual Studio Code、Supabase 以及 Pano2VR 進行開發說明：

**(1). 平台設計和需求分析**：藉由參考各大院校官網、Google Maps 地圖引導以及現代線上點餐平台的版面設計方式，進行整體的版面規劃，分析本校師生與訪客所需的實用功能（如空間檢索、宿舍環境導航、無接觸模擬點餐與後台訂單控管）。

**(2). 選擇開發平台**：使用 **Visual Studio Code (VS Code)** 作為主要開發 IDE，進行網頁系統的設計與開發。系統採用輕量級的前後端分離架構，前端使用 HTML5、CSS3 與原生 JavaScript，並引進 Supabase Client SDK 直連雲端服務，免去架設傳統後端伺服器的複雜度。

**(3). 資料庫管理**：使用 **Supabase (PostgreSQL 引擎)** 雲端主控台設計資料庫結構，建立 `stores`（店家）、`xuebei_menu` / `qiqi_menu` / `juna_menu`（各餐廳菜單）、`購物車主表`（訂單主表）與`購物車明細表`（訂單明細）等資料表，配置外鍵關聯以確保資料一致性，並配置資料列級安全性原則（RLS）來防範未授權的修改。

**(4). 測試**：實施前端介面、全景節點連動與資料庫 API 讀寫測試，確保系統的各個部分（如搜尋框精確跳轉、購物車結帳寫入、後台訂單明細讀取）都能正確運行。我們在本地端使用的是 **XAMPP (Apache HTTP Server)** 來啟動網頁伺服器並進行網頁功能的預覽與除錯。

**(5). 部署**：將網頁系統部屬至 Apache 本地伺服器中（或可上傳至 GitHub Pages / Vercel 等靜態雲端託管平台），確保 API 連線金鑰配置正確，使應用程式能夠在各種行動裝置與桌機瀏覽器上順暢運行。

> **圖10. 研究方法與步驟示意圖**

---

## 4、開發過程介紹
**(1). 從選定語法及開發環境後，開始設計前端網頁畫面，並規劃網頁功能**。我們以「校園實用性」與「沉浸式導覽」為核心，盡可能地精簡與優化網站功能，避開冗餘設計以防止模糊焦點，最終聚焦在「3D 實景定位檢索」以及「餐廳線上模擬點餐」這兩大能切實解決新生與訪客痛點的核心功能板塊。

**(2). 設計網站架構雛形，排版、功能位置、所需功能列表**。先設計好歡迎封面、VR 全景播放區域、行事曆公告與精選卡片的版面，使用 Pano2VR 軟體進行 Insta360 全景照片的拼接、設定導覽箭頭熱點與關聯，完成整個靜態網頁系統的排版後，再開始處理與雲端資料庫對接的動態功能設計。

**(3). 前端全景定位與資料庫連線處理**。藉由 JavaScript 非同步呼叫 Supabase API 載入美食餐廳列表、學生宿舍規格與首頁留言列表，並透過 DOM 動態渲染於網頁上；同時使用 JavaScript 陣列結構實作本地端購物車的商品增減與金額動態累加。

**(4). 結帳送單處理**。當使用者在餐廳頁面完成選餐並點擊結帳時，系統會收集購物車清單並提示輸入姓名，呼叫 Supabase API 以非同步方式，將一筆新訂單記錄寫入「購物車主表」中，並依據回傳的訂單 ID 循環將細項寫入「購物車明細表」中，完成雲端訂單資料的新增（Create）。

**(5). 後台訂單控管與留言置頂功能**。管理員進入後台系統時，JS 會自動讀取資料庫訂單資料進行今日營收與件數的即時累加統計，並可在點選「完成」時執行 `UPDATE` 更新資料表 status；首頁與論壇留言板亦提供「置頂」按鈕，前端可向 Supabase 更新對應留言的 `is_pinned` 欄位以控制排序，實現即時看板管理。
