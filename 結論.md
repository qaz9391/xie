# 明新科技大學 校園導覽與線上點餐系統 — 專案結論

## 結論

這個專案讓我學到了許多寶貴的經驗與技能。其中，我深入研究了雲端資料庫與前端網頁的直接連結，這是在課堂上並不常見的議題。前後端分離架構與 serverless（無伺服器）的對接技術也是我在這個專案中所學習到的，而這些技術在學校的課程中並未涉及，迫使我需要額外的自學與探索。這段時間內，我投入了大量的時間去學習異步程式設計與 API 串接，因為這與我過去所學的程式碼有所不同，特別是在處理各種不同的 JavaScript 異步處理（如 `async/await` 與 `Promise`）以及 Supabase Client SDK 類別庫程式碼時，這些都是在平常的課程中很少涉及的部分。我之前學習的大部分內容都集中在基本的本地邏輯運算或簡單的 UI 設計，但在做網頁與 3D VR 整合開發時，我發現 Web 開發擁有極為多樣且強大的 JavaScript 類別庫與雲端 API 資源，這讓我感到相當驚訝。這次的專案讓我深入了解到在實際應用中，現代 JavaScript 的生態圈與雲端服務（如 Supabase 的 Row Level Security）是多麼的豐富和實用。這段寶貴的學習經驗讓我更加確信，在未來的學習和工作中，持續不斷地學習和探索新興科技是取得成功的關鍵。
