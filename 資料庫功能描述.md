# 資料庫功能與 CRUD 運作說明 (專題適用版)

本文件將資料庫的學術理論（如 CRUD 操作、SQL 機制、資料安全）與專題系統中的**具體程式檔案、資料表及實際運作邏輯**進行深度結合。您可以直接將以下內容複製並貼上至您的專題報告、論文或簡報中。

---

## 2-3-1 資料庫設計與系統架構
本校園導覽系統採用雲端資料庫管理系統 **Supabase (PostgreSQL 引擎)** 作為核心資料儲存平台。透過在雲端定義結構化的關聯式資料表，並配置**資料列級安全性原則（Row Level Security, RLS）**，實現前端 JavaScript (透過 Supabase API) 在進行直接安全存取的同時，亦能保障資料的一致性與防竄改機制，徹底達成前後端分離的系統架構。

---

## 2-3-2 CRUD 核心操作於 VR 導覽之實踐
本導覽系統的核心運作邏輯（包括空間檢索、宿舍導覽、點餐系統及留言板互動）均建構於資料庫之 CRUD（新增、查詢、更新、刪除）四項基本操作之上。以下結合系統內**實際的程式檔案、資料表與實務運作流程**進行具體說明：

### 1. 新增 (Create)：使用者互動寫入與系統初始化
新增操作負責將前端使用者輸入之動態資料寫入資料庫，以及在後端進行系統初始空間設定。
*   **實務運作與檔案對應**：
    *   **留言板新增**：使用者在 VR 導覽頁面的留言板（`vr_forum.html`）發表評論時，前端指令腳本 `js/vr_forum.js` 會呼叫 `supabase.from('vr_comments').insert()`，將訪客名稱、內容與時間即時新增至 `vr_comments` 資料表。同理，美食論壇的留言則透過 `js/cafeteria.js` 寫入 `comments` 資料表。
    *   **點餐結帳**：當使用者在餐廳頁面（如 `xuebei_restaurant.html`）完成選餐並送出訂單時，系統透過 `js/orders.js` 執行 `INSERT` 指令，將消費主檔與明細分別寫入 `orders` (訂單主表) 與 `order_items` (訂單明細表) 中。
    *   **空間資料載入**：開發者利用 `database/add_campus_locations.sql` 與 `database/add_dorm_data.sql` 批次執行 SQL `INSERT` 語句，將多達 800+ 筆校園空間、科系以及宿舍房間資訊（`dorm_rooms`）寫入資料庫中。

### 2. 查詢 (Read)：空間定位檢索與資訊動態渲染
查詢是系統中最頻繁的行為，主要處理使用者導覽搜尋與頁面動態內容的載入。
*   **實務運作與檔案對應**：
    *   **VR 導覽定位**：當使用者於主介面搜尋框輸入關鍵字（如「資管系辦」）時，系統從 `campus_locations` 資料表檢索該地點對應的 `vr_node`（例如 `glxy` 代表管理學院），並驅動 Pano2VR 撥放器（讀取 `360VR導覽.p2vr` 導出之全景靜態檔案）無縫跳轉至該全景畫面。
    *   **宿舍資訊動態渲染**：使用者進入宿舍導覽（`index1.html`）時，系統會透過 API 查詢 `dorm_rooms` (房型)、`dorm_facilities` (設施) 與 `dorm_accordions` (申請須知)，將價格、房型說明及設施圖示動態渲染於網頁，避免靜態網頁硬編碼的維護困難。
    *   **美食餐廳與菜單加載**：`cafeteria.html` 配合 `js/cafeteria.js` 從 `stores` 載入餐廳清單；當使用者切換至特定店家時，`js/menu.js` 會根據該店家 ID 從 `menu_items` 資料表查詢對應的餐點品項、價格及是否供應（`is_available`）狀態。

### 3. 更新 (Update)：即時資訊維護與狀態追蹤
更新操作確保系統內容的時效性與互動功能之管理，管理者無須更動 VR 底層全景圖或重新編譯前端程式碼。
*   **實務運作與檔案對應**：
    *   **校園空間異動**：若某科系辦公室搬遷，管理者直接於資料庫中更新 `campus_locations` 表中的 `description` (描述文字) 與 `building` (所在大樓)，導覽網頁即可即時呈現最新地址與定位，完全不需重新更換 VR 底層全景圖檔（`360VR導覽.p2vr`）或重新部署代碼。
    *   **留言置頂控制**：管理者可透過後端或指令修改 `vr_comments` 或 `comments` 資料表中的 `is_pinned` 欄位（由 `false` 改為 `true`），使特定留言在前端顯示時，透過 JS 邏輯強制置頂展示。
    *   **餐點與訂單狀態更新**：當商家修改菜單或管理員確認訂單時，透過更新 `menu_items` 的價格或 `orders` 的 `status` 欄位（如將 `pending` 改為 `completed`），實現系統營運流程。

### 4. 刪除 (Delete)：過時數據清理與系統重設
刪除操作用於清理系統冗餘、錯誤資訊，並進行資料初始化維護。
*   **實務運作與檔案對應**：
    *   **留言板淨化**：為維護社群平台秩序，管理員可在 Supabase 控制台或後台管理介面中，刪除 `vr_comments` 或 `comments` 表中不當或過時的言論，維護系統資訊的正確性與安全性。
    *   **系統測試數據重設**：在執行 SQL 初始化配置時（如執行 `database/add_campus_locations.sql` 與 `database/add_dorm_data.sql`），會先利用 `TRUNCATE TABLE ... RESTART IDENTITY` 或 `DELETE` 清除測試用的舊資料，避免主鍵衝突或重疊寫入，確保資料表只保存最精確的系統運行資料。

---

## 2-3-3 專題檔案與資料庫操作對照表

以下整理本系統中主要檔案、操作的資料表，以及在 CRUD 中扮演的角色：

| 檔案路徑與名稱 | 運作功能用處 | 影響之資料表 (Tables) | 對應之 CRUD 操作 |
| :--- | :--- | :--- | :--- |
| `backend/schema.sql` | 初始化資料庫結構，設定資料表關聯與 RLS 安全策略。 | `stores`, `menu_items`, `orders`, `order_items`, `comments`, `vr_comments` | 結構定義 (DDL) |
| `database/add_campus_locations.sql` | 預先導入校園各大樓、學院、系所與行政單位空間資料，並關聯 VR 節點。 | `campus_locations` | 新增 (C) / 刪除 (D) |
| `database/add_dorm_data.sql` | 初始化宿舍房型規格、公共設施清單、聯絡資訊與申請公告細節。 | `dorm_rooms`, `dorm_facilities`, `dorm_contacts`, `dorm_accordions` | 新增 (C) / 刪除 (D) |
| `js/vr_forum.js` | 控制 VR 導覽主留言板。讀取並置頂顯示留言，以及發送新增留言請求。 | `vr_comments` | 查詢 (R) / 新增 (C) |
| `js/cafeteria.js` & `js/menu.js` | 渲染學生餐廳店家與對應菜單。動態查詢餐點、單價與供應狀態。 | `stores`, `menu_items` | 查詢 (R) |
| `js/orders.js` | 處理線上點餐結帳，將前端購物車轉為資料庫訂單記錄。 | `orders`, `order_items` | 新增 (C) |
