# 明新校園導覽系統 — 圖表資料彙整 (繁體中文版)

本文件整理了繪製 **循序圖 (Sequence)**、**實體關聯圖 (ERD)**、**活動圖 (Activity)** 與 **案例圖 (Use Case)** 所需的所有詳細資料與資料庫對應關係。

---

## 一、 案例圖資料 (Use Case Diagram)
**參與者 (Actors)**：
- **一般使用者 (學生/訪客)**：主要操作導覽與點餐功能。
- **管理員**：負責後台訂單維護與留言管理。

**案例清單與功能說明**：
| 案例名稱 | 參與者 | 說明 | 資料來源 / 資料表 |
|---------|-------|------|-----------------|
| 瀏覽 VR 導覽 | 使用者 | 在首頁旋轉、縮放、切換場景 | `output/`, `output_index1/output/` 靜態檔案 |
| 搜尋校園地點 | 使用者 | 搜尋大樓、處室、教室 | `home.html` (硬編碼 `campusLocations`) |
| 查看行事曆 | 使用者 | 查看 114 學年度重要日程 | `home.html` (硬編碼 `eventsData`) |
| 瀏覽美食店家 | 使用者 | 查看推薦餐廳清單 | `店家` 表格 |
| 發表美食留言 | 使用者 | 在論壇區對店家發表評論 | `美食留言` 表格 |
| 店家點餐 | 使用者 | 選擇餐點、加入購物車並結帳 | `雪杯工坊菜單`, `奇奇菜單`, `焗奶奶菜單` |
| 送出訂單 | 使用者 | 填寫姓名並送出購買清單 | `購物車主表`, `購物車明細表` 表格 |
| 管理訂單 | 管理員 | 查看所有訂單、更新狀態 (完成/待處理) | `購物車主表`, `購物車明細表` 表格 |
| 管理留言 | 管理員 | 置頂或取消置頂美食留言 | `美食留言` 表格 (更新 `is_pinned`) |

---

## 二、 實體關聯圖資料 (ER Diagram)
本系統使用 **Supabase (PostgreSQL)** 作為後端，主要關聯如下：

### 1. 核心業務邏輯 (訂餐與店家)
- **`店家` (商店/餐廳)**
    - `id` (UUID PK): 店家唯一識別碼
    - `name` (TEXT): 餐廳名稱
    - `link_url` (TEXT): 導向的 HTML 檔案 (如 `xuebei_restaurant.html`)
    - `image_url` (TEXT): 餐廳封面照
- **`雪杯工坊菜單` / `奇奇菜單` / `焗奶奶菜單` (菜單)**
    - `id` (BIGINT PK): 餐點 ID
    - `name` (TEXT): 餐點名稱
    - `price` (INT): 價格
    - `category` (TEXT): 分類 (如：吐司、漢堡、主菜)
    - `type` (TEXT): `single` (單點) / `combo` (套餐)
- **`購物車主表` (訂單主表)**
    - `id` (UUID PK): 訂單編號
    - `customer_name` (TEXT): 訂購人姓名
    - `total_amount` (INT): 總金額
    - `status` (TEXT): `pending` (待處理) / `completed` (完成)
- **`購物車明細表` (訂單明細)**
    - `id` (PK): 明細 ID
    - `order_id` (UUID FK): 關連到 `購物車主表.id`
    - `menu_item_id` (INT): 餐點 ID
    - `quantity` (INT): 數量

### 2. 社群互動 (論壇)
- **`美食留言` (美食論壇)**
    - `id`, `user_name`, `content`, `is_pinned`, `created_at`
- **`首頁留言板` (VR 導覽論壇)**
    - `id`, `user_name`, `content`, `is_pinned`, `created_at`

### 3. 宿舍管理 (Dormitory)
- 使用表：`住宿房間資料`, `住宿各類設施`, `住宿地址資料`, `住宿各式資料1`

---

## 三、 活動圖資料 (Activity Diagram)

### [使用者活動 A：首頁導覽流程]
1. **開始**
2. **進入首頁 (`home.html`)**
3. **分支出發**：
    - **途徑 1**：操作 VR 視窗 (全景旋轉)
    - **途徑 2**：輸入關鍵字搜尋 (檢索 `campusLocations` 陣列)
    - **途徑 3**：點擊「學生餐廳」卡片 -> **轉向活動 B**
    - **途徑 4**：點擊「新生宿舍」卡片 -> **進入 `index1.html`**

### [使用者活動 B：線上點餐流程]
1. **進入 `cafeteria.html`**
2. **讀取資料庫**：從 `店家` 載入店家
3. **選擇店家** (如：雪杯工坊)
4. **進入餐廳頁**：載入 `雪杯工坊菜單` 菜單
5. **點選餐點**：加入前端購物車 (JS Array: `cart`)
6. **點擊結帳**：
    - 輸入姓名
    - **資料庫動作**：`INSERT` 到 `購物車主表`
    - **資料庫動作**：`INSERT` 到 `購物車明細表`
7. **顯示成功頁面**

---

## 四、 循序圖資料 (Sequence Diagram)
以下是「結帳」過程的物件互動順序：

1. **使用者 (User)** 點擊餐廳頁面的「送出訂單」按鈕。
2. **頁面腳本 (Inline JS)** 收集購物車中的商品清單。
3. **頁面腳本** 調用 `supabase.from('購物車主表').insert()`：
    - 建立一筆主訂單。
    - **回傳** 訂單 `id`。
4. **頁面腳本** 調用 `supabase.from('購物車明細表').insert()`：
    - 循環將購物車每筆項目的 `order_id` 設為剛取得的 `id`。
5. **Supabase** 確認寫入完畢並回傳 success。
6. **頁面腳本** 清空本地 `localStorage` 的購物車。
7. **頁面腳本** 彈出 `alert("訂單已送出")` 並跳轉/重新整理。

---

## 五、 技術備註：資料來源位置 (Source of Truth)
| 元件名稱 | 資料來源位置 | 備註 |
|---------|------------|------|
| 老師資料 | `home.html` (Javascript: `const campusLocations`) | **[重要]** 之前在資料庫，現在已改為前端硬編碼 |
| 首頁公告 | `home.html` (HTML: `marquee`) | 靜態內容 |
| 行事曆日程 | `home.html` (Javascript: `const eventsData`) | 硬編碼內容 |
| 店家連結 | `店家` 表 (欄位 `link_url`) | 決定點擊店家後跳轉哪個 HTML 頁面 |
| 宿舍資訊 | `住宿房間資料`, `住宿各式資料1` 等表 | 由 `index1.html` 透過 Supabase 讀取 |
| 訂單計算 | `xuebei_restaurant.html` (Javascript: `updateCartUI`) | 前端即時計算總金額後再寫入資料庫 |
