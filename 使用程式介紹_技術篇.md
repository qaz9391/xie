# 明新科技大學 校園導覽與線上點餐系統 — 使用程式與開發工具介紹

## 2.4、使用程式介紹
本環節介紹我們所使用的語法及使用的開發環境(IDE)與相關軟硬體工具。

### Visual Studio Code 介紹
Visual Studio Code（簡稱 VS Code）是一款由微軟開發的免費、開源且跨平台的整合開發環境（IDE）。它具備輕量化、啟動速度快且擴充性極佳的特點，在現代 Web 前端開發中被廣泛採用。VS Code 內建了強大的語法高亮、智能程式碼補全（IntelliSense）、Git 版本控制整合，並擁有豐富的套件市場（如 Live Server、Prettier 等），能大幅優化 HTML、CSS 與 JavaScript 的編寫體驗，是本專案前端開發的核心工具。
 
> **圖2. Visual Studio Code 開發介面**

---

### Supabase (PostgreSQL 雲端資料庫) 介紹
Supabase 是一款開源的後端即服務（Backend-as-a-Service, BaaS）平台，基於強大的 PostgreSQL 關聯式資料庫構建。它提供了即時的 RESTful API、自動化的資料庫結構生成以及安全可靠的資料列級安全性原則（Row Level Security, RLS）。在本專案中，Supabase 取代了傳統自建的 MySQL 資料庫，負責儲存餐廳店家、菜單明細、點單主表、訂單明細以及首頁與論壇留言。開發者無需自行編寫複雜的後端連線 API，即可透過 Supabase JS Client SDK 在前端直接進行安全、高效的 CRUD 資料存取。

> **圖3. Supabase 資料庫後台管理介面**

---

### Supabase JavaScript SDK 介紹
Supabase JavaScript Client SDK 是用於讓前端網頁與 Supabase 雲端資料庫進行通訊的官方套件。它封裝了複雜的 HTTP 請求，使開發者能使用直覺的 JS 語法（例如 `.select()`、`.insert()`、`.update()`、`.eq()`）對資料庫進行操作。這避免了在網頁中暴露敏感的 SQL 連線字串，並保障了前後端資料流的安全傳輸。

> **圖4. 透過 Supabase SDK 進行資料庫 Insert 新增訂單程式碼**

---

### HTML5 介紹
HTML（HyperText Markup Language）是構建網頁結構與內容的最基礎標記語言。本專案採用最新的 HTML5 標準，透過語義化標籤（如 `<header>`、`<section>`、`<nav>`）來組織導覽首頁（`home.html`）、點餐頁面（`cafeteria.html`）與後台訂單檢視頁面（`orders.html`）。HTML5 的多媒體支持與自適應特性，為我們嵌入 3D 全景虛擬實境畫面與餐廳互動卡片奠定了堅實的基礎。

> **圖5. HTML5 網頁結構程式碼**

---

### CSS3 介紹
CSS（Cascading Style Sheets）是用於控制網頁外觀樣式與佈局的樣式表語言。本專案利用 CSS3 進行視覺美化與版面配置，包含字型設定（Google Fonts）、Flexbox/Grid 彈性佈局以確保網頁在手機與電腦版皆能完美呈現。同時，透過 CSS3 的過渡效果（Transition）與動畫（Animation），我們為點餐購物車、搜尋視窗及導覽卡片設計了流暢的微動畫，極大地提升了介面的親和力與視覺質感。

> **圖6. CSS3 樣式與動畫設定程式碼**

---

### JavaScript (ES6+) 介紹
JavaScript 是一種高級的、解釋性的程式語言，主要用於在網頁上實現互動式邏輯與動態資料渲染。在我們的專案中，JavaScript 扮演著「大腦」的角色：它負責操作 DOM 元素以響應使用者的點擊與搜尋、管理本地購物車資料（`localStorage`）、與 Supabase API 進行異步資料交換（`async/await`），並驅動 Pano2VR 播放器進行 VR 場景定位跳轉。

> **圖7. JavaScript 異步讀取資料庫留言與置頂邏輯程式碼**

---

### XAMPP (Apache HTTP Server) 介紹
XAMPP 是一個易於安裝的 Apache 發行版本，其中包含 MariaDB、PHP 和 Perl。在本專案中，我們主要使用 XAMPP 內建的 **Apache HTTP 伺服器** 作為本地端的靜態與動態網頁伺服器。一旦 Apache 伺服器啟動，開發團隊即可將專案放置於 `htdocs` 目錄中，並透過瀏覽器輸入 `http://localhost/myvr/home.html` 進行本地端的功能測試與調試，模擬真實網頁伺服器的運行環境。

> **圖8. XAMPP 控制台與 Apache 伺服器啟動狀態**

---

### Pano2VR 與 Insta360 Studio 介紹
Pano2VR 是一款功能強大的全景圖轉換與虛擬導覽製作軟體。在本專案中，我們首先使用 **Insta360 Studio** 對相機拍攝的原生全景素材進行拼接與高解析度導出，隨後將全景靜態照片匯入 **Pano2VR**。在其中，我們定義了空間節點、設定導覽熱點（Hotspots）、建立大樓與處室的關聯座標，並最終輸出為支援 HTML5 與 WebGL 渲染的互動式 3D 全景導覽模組，嵌入至系統主頁中，供使用者進行 720° 的實境探索。

> **圖9. Pano2VR 虛擬導覽節點規劃與關聯設定介面**
