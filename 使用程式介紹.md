# 明新科技大學 校園導覽與線上點餐系統 — 使用程式介紹

## 3、系統操作與使用程式說明

為了讓使用者與專題評審能夠快速理解「校園導覽與線上點餐系統」的技術架構與操作方式，本章節將針對系統的環境建置、核心程式碼功能、前端頁面運作及資料庫的對接機制進行深度說明。

---

### 3.1、系統執行與開發環境準備
本專案採用輕量級的前後端分離架構，前端利用 HTML5、CSS3 與原生 JavaScript 進行介面開發與動態渲染，後端及資料庫則使用 Supabase 雲端資料庫（PostgreSQL 引擎）。因此，在本地端執行本系統時，必須完成以下環境準備：
1. **本地網頁伺服器架設**：
   將專案資料夾整個放置於 Apache (如 XAMPP) 的 Web 根目錄下（例如 `d:/xampp_new/htdocs/myvr`）。啟動 Apache 伺服器後，即可透過瀏覽器輸入下方網址進入系統：
   - **前台導覽與點餐主頁**：[http://localhost/myvr/home.html](http://localhost/myvr/home.html)
   - **後台訂單管理看板**：[http://localhost/myvr/orders.html](http://localhost/myvr/orders.html)
2. **Supabase 雲端資料庫設定**：
   系統所有動態資料（如店家菜單、訂單記錄、留言板留言）均儲存於雲端資料庫中。專案目錄下設有資料庫結構初始化腳本，開發者需在 Supabase 控制台的 SQL Editor 中執行以下腳本以建立對應的資料表與關聯：
   - [schema.sql](file:///d:/xampp_new/htdocs/myvr/backend/schema.sql)
   - [add_campus_locations.sql](file:///d:/xampp_new/htdocs/myvr/database/add_campus_locations.sql)
   - [add_dorm_data.sql](file:///d:/xampp_new/htdocs/myvr/database/add_dorm_data.sql)
3. **資料庫連線設定**：
   系統的資料庫連線參數皆統一管理於設定檔 [config.js](file:///d:/xampp_new/htdocs/myvr/js/config.js) 中。在此檔案中定義了 Supabase 的 Project URL 與 Anonymous Key，供所有前端 JS 腳本共享呼叫：
   ```javascript
   const CONFIG = {
       SUPABASE_URL: 'https://xodnrtfnzblgxupfbmka.supabase.co',
       SUPABASE_KEY: 'sb_publishable_cgLu4SOQhHgYzJmbgRbkYg_fE3_cRz9'
   };
   window.CONFIG = CONFIG;
   ```

---

### 3.2、核心前端網頁與操作說明
本系統主要由以下幾個核心網頁與功能模組組成：
- **(1) 校園導覽與首頁留言板 (`home.html` + `js/vr_forum.js`)**：
  這是系統的首頁。頂部嵌入了以 Insta360 拍攝並透過 Pano2VR 轉檔的 3D 全景（VR）導覽播放視窗，提供使用者 720° 的沉浸式空間體驗。首頁的搜尋功能允許使用者輸入「大樓名稱」、「處室」或「教師姓名」等關鍵字進行快速定位，系統會比對內建的 `campusLocations` 陣列，並驅動全景播放器即時跳轉至對應的位置場景。
  此外，首頁下方配置了**首頁留言板**。當使用者在此發表留言時，系統會調用 [vr_forum.js](file:///d:/xampp_new/htdocs/myvr/js/vr_forum.js) 透過 Supabase API 將留言資料寫入「首頁留言板」資料表，並透過 DOM 進行即時渲染。管理員可以直接在網頁上針對留言進行「置頂」與「取消置頂」操作，修改資料庫中的 `is_pinned` 狀態，達成即時互動管理。

- **(2) 新生宿舍導覽專區 (`index1.html` + `output_index1/`)**：
  專為新生設計的宿舍導覽頁面，整合了宿舍公共區域與房型內部實景導覽。網頁會透過 Supabase 讀取 `dorm_rooms` (宿舍房間資料) 與公共設施清單，將房間規格、各樓層設備與申請須知等實用資訊動態渲染於網頁上，使新生在入住前即可對生活環境有全面的掌握。

- **(3) 線上模擬點餐前台 (`cafeteria.html` + 各餐廳專屬頁面 + `js/cafeteria.js` + `js/menu.js`)**：
  使用者點選首頁的「學生餐廳」後，會轉向點餐前台頁面。此處會自資料庫動態載入店家清單與菜單資訊：
  - [cafeteria.js](file:///d:/xampp_new/htdocs/myvr/js/cafeteria.js) 負責控制購物車系統，支持商品的增加、減少及總金額的即時計算。
  - 當使用者點擊「送出訂單」時，系統會提示輸入訂購人姓名，並執行 `INSERT` 指令，將訂單主檔與訂單明細分別寫入 `購物車主表` (orders) 與 `購物車明細表` (order_items) 中，完成線上模擬點餐流程。

- **(4) 後台訂單管理看板 (`orders.html` + `js/orders.js`)**：
  專為餐廳業者或系統管理員設計的後台管理介面。當管理員進入 [orders.html](file:///d:/xampp_new/htdocs/myvr/orders.html) 時，後台腳本 [orders.js](file:///d:/xampp_new/htdocs/myvr/js/orders.js) 會向 Supabase 發起 `SELECT` 請求，拉取所有消費訂單記錄，並即時統計「今日訂單總數」、「待處理訂單數」與「今日營業額」。
  管理員可點擊「查看明細」按鈕，系統會關聯查詢 `購物車明細表` 與 `menu_items` 資料表，顯示該筆訂單所點選的具體餐點名稱與數量；同時，管理員可點擊「完成」按鈕，執行 `UPDATE` 將該訂單的 `status` 欄位更新為 `completed`，方便廚房與櫃檯即時控管訂單進度。

---

### 3.3、資料庫表結構與 CRUD 操作對照
系統中各程式模組與雲端資料表之間的互動關係如下表所示：

| 模組功能 | 調用之 JavaScript 程式碼 | 異動之資料表 (Supabase) | 使用之 SQL / API 操作 |
| :--- | :--- | :--- | :--- |
| **首頁留言板管理** | [vr_forum.js](file:///d:/xampp_new/htdocs/myvr/js/vr_forum.js) | `首頁留言板` (vr_comments) | `SELECT` (載入留言)<br>`INSERT` (發送留言)<br>`UPDATE` (切換置頂狀態) |
| **餐廳菜單加載** | [menu.js](file:///d:/xampp_new/htdocs/myvr/js/menu.js) | `stores`, `menu_items` (菜單明細) | `SELECT` (依據店家 ID 動態讀取餐點) |
| **模擬送出點餐** | [cafeteria.js](file:///d:/xampp_new/htdocs/myvr/js/cafeteria.js) | `購物車主表`, `購物車明細表` | `INSERT` (主訂單與明細批次寫入) |
| **後台訂單控管** | [orders.js](file:///d:/xampp_new/htdocs/myvr/js/orders.js) | `購物車主表`, `購物車明細表` | `SELECT` (拉取所有訂單進行統計)<br>`UPDATE` (完成/取消訂單狀態) |
